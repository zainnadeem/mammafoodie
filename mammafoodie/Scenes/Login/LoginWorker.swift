import UIKit

class LoginWorker {
    // MARK: - Business Logic
    func login(with email: String, password: String, completion: (()->Void)) {
        completion()
    }
    func logout(with email: String, password: String, completion: (()->Void)) {
        completion()
    }
}
